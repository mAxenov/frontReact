export const admin = ['admin'];
export const manager = ['admin', 'manager'];
export const client = ['admin', 'manager', 'client'];
export const noRole = ['admin', 'manager', 'client', 'noRole'];

export const roles = {
  admin,
  manager,
  client,
};
